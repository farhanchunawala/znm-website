'use client';

export default function CustomersTestPage() {
	return (
		<div style={{ padding: '20px' }}>
			<h1>Customers Page Test</h1>
			<p>If you see this, the page renders without the error.</p>
			<p>The error is likely coming from a parent component or layout.</p>
		</div>
	);
}
